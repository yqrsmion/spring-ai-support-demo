# 协作说明（供 Codex 阅读）

本工程是学习演示工程。用户的目的是快速掌握 Spring AI 的工作原理与核心组件，
视角偏向企业级应用，可随学习进度逐步扩展代码。

## 已完成功能（当前版本）

- 基础链路：ChatClient、会话记忆（conversationId）、@Tool 工具调用、SSE 流式（chunk + full）、非流式 JSON；
- 企业级硬化：令牌桶限流、超时与异常降级（友好提示）、traceId 链路日志、全局异常兜底；
- 模型网关：chat / reasoner / flash / pro 多模型路由（`&model=` 或 `/model` 命令），失败自动降级；
- 会话持久化：本地 JSON 文件（`data/memory/`），重启不丢；
- 简化版 RAG：本地文档切块检索（`docs/support-policy.md`）并注入上下文；
- 测试：17 个单元测试，`mvn test` 全绿；
- 内置网页聊天界面：`http://localhost:8080`。

## 协作约定（务必遵守）

- Git 提交信息一律使用中文，并写明本次改了什么。
- 提交前先执行 `git status` / `git diff`，只暂存本次任务相关文件；不要用 `git add -A`
  全量添加，避免把用户在 IDEA 里手动改的文件（或误输入的字符）一起提交。
- 用户可能在 IDEA 中误输入字符（例如 CSS 里多出 1111），提交前留意工作区差异。
- 模型 API Key 一律通过环境变量 DEEPSEEK_API_KEY 提供，不写入代码、文档或 Git。
- 工程内不得包含任何个人信息或真实业务数据。
- 交付时结合代码讲解工作原理、组件与调用链，不必逐行展开；文档需包含可复现的验证方式。
